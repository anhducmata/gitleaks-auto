name: test

on:
  push:
  pull_request:

jobs:
  hook-regression:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: brew install gitleaks
      - run: bash tests/test.sh
